---
layout: null
---
[
{% for ch in site.chapters %}
  {
    "title": {{ ch.title | jsonify }},
    "short": {{ ch.short_title | default: ch.title | jsonify }},
    "desc": {{ ch.description | default: "" | jsonify }},
    "cat": {{ ch.category | default: "" | jsonify }},
    "date": {{ ch.date | date: "%Y-%m-%d" | jsonify }},
    "url": {{ ch.url | relative_url | jsonify }}
  }{% unless forloop.last %},{% endunless %}
{% endfor %}
]
